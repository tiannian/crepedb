[package]
name = "crepedb-redb"
version = "0.1.0"
edition = "2021"
description.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
crepedb = { workspace = true, features = ["tests"] }

log.workspace = true

redb = "1.5.0"

[dev-dependencies]
env_logger = "0.11.2"
