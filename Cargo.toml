[workspace]
resolver = "2"
members = ["crepedb", "tool"]

[workspace.dependencies]
log = "0.4.20"
hex = { version = "0.4.3", default-features = false, features = ["alloc"] }

# utils
seq-macro = "0.3.5"

crepedb = { path = "crepedb", version = "0.1" }
